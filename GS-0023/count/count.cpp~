#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
  freopen("count.in", "r", stdin);
  freopen("count.out", "w", stdout);

  int num;
  cin >> num;

  int bag[200];

  for (int i = 0; i < num; i++)
  {
    cin >> bag[i];
  }

  
  return 0;
}
