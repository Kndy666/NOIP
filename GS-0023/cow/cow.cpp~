#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
  freopen("cow.in", "r", stdin);
  freopen("cow.out", "w", stdout);

  int n, d, arr[];
  cin >> n >> d;

  for (int i = 0; i < n; i++)
  {
    cin >> arr[i];
  }

  
}
